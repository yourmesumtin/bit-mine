apiVersion: apps/v1
kind: Deployment
metadata:
  name: bitcoin-core-deployment
spec:
  replicas: 2  # Ensuring very high availability
  selector:
    matchLabels:
      app: bitcoin-core
  template:
    metadata:
      labels:
        app: bitcoin-core
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - name: bitcoin-core
        image: yourme/bitcoin-core:c55c832c842c74a9d397ade208dfb76f6bb8dd2c
        ports:
        - containerPort: 8332
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1"
        volumeMounts:
        - name: bitcoin-data
          mountPath: /bitcoin
      volumes:
      - name: bitcoin-data
        persistentVolumeClaim:
          claimName: bitcoin-data-pvc
